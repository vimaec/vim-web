/**
 * Re-export of vim-format BIM types.
 * Bundled into dist/vim-bim.d.ts as a self-contained reference.
 */
export type {
  IElement,
  ILevel,
  IPhase,
  ICategory,
  IWorkset,
  IDesignOption,
  IView,
  IGroup,
  IAssemblyInstance,
  IBimDocument,
  IRoom,
  IFamily,
  IFamilyType,
  IFamilyInstance,
  IMaterial,
  INode,
  VimDocument,
  VimHeader,
} from 'vim-format'

export { VimHelpers } from 'vim-format'
